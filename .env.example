# AgentPhone
AGENT_PHONE_KEY=
AGENTPHONE_AGENT_ID=       # greeter agent (main number) — also used as demo agent until greeter is built
AGENTPHONE_ONBOARDING_AGENT_ID=   # onboarding agent — add once Step 3 of plan_v1 is built
AGENTPHONE_NUMBER=         # main phone number attached to greeter agent

# Gemini / Google
GEMINI_API_KEY=
GOOGLE_API_KEY=

# AgentMail
AGENTMAIL_API_KEY=

# ngrok
NGROK_WEBSITE=https://your-domain.ngrok-free.dev

# Ports (defaults shown — only change if you have conflicts)
PORT_BACKEND=8000
PORT_PLUMBER=3100
PORT_STYLIST=3101
PORT_ONBOARDING=3010
